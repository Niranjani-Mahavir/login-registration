export interface LoginDataModel {
    email: string;
    password: string;
}

export interface SignupDataModel {
    username: string;
    email: string;
    password: string;
    confirmPassword: string;
}
